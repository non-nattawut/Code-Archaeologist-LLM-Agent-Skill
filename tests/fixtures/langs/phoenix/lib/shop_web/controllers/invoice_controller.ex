defmodule ShopWeb.InvoiceController do
  @moduledoc "Invoices, routed by `resources`, only: [:index, :show]."

  @doc "List invoices."
  def index(conn, _params) do
    conn
  end

  @doc "Show one invoice."
  def show(conn, _params) do
    conn
  end
end
